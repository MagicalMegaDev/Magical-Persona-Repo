#constants.gd
#Holds all constants used across multiple scripts

extends Node

const BASE_KNOCKBACK_STRENGTH := 100.0 #the baseline of all knockback
const CONTROLLER_DEADZONE := 0.3 #Slightly higher than the default to prevent stick drift
